adwords:
  #############################################################################
  # Required Fields                                                           #
  #############################################################################
  # Берем developer_token из интерфейса самого AdWords
  developer_token: 123-123-123b,ergl 
  #############################################################################
  # Optional Fields                                                           #
  #############################################################################
  # client_customer_id - нужный подаккаунт. НЕ MCC - т.к. в нем нет кампаний
  client_customer_id: 929-645-8000
  user_agent: UtAds_MCC
  # partial_failure: True
  # validate_only: True
  #############################################################################
  # OAuth2 Configuration                                                      #
  # Below you may provide credentials for either the installed application or #
  # service account flows. Remove or comment the lines for the flow you're    #
  # not using.                                                                #
  #############################################################################
  # The following values configure the client for the installed application
  # flow.
  # Генерим в Google Cloud -> API и сервисы -> Учетные данные -> Идентификаторы клиентов OAuth 2.0
  client_id: 78997227044-nd5ep6qdieo9tgvmn7te2jvgb36k.apps.googleusercontent.com
  client_secret: 1rpr-0000000
  # запускаем скрипт generate_refresh_token.py копируем из браузера код, вставляем его в code:  жмем Enter и получаем  refresh_token
  refresh_token: 1/000-y0-000dRSQ-0009t700
  # The following values configure the client for the service account flow.
  # service_account_email:  78997227044-compute@developer.gserviceaccount.com
  # path_to_private_key_file: INSERT_PATH_TO_FILE_HERE
  # delegated_account: INSERT_DOMAIN_WIDE_DELEGATION_ACCOUNT
  #############################################################################
  # ReportDownloader Headers                                                  #
  # Below you may specify boolean values for optional headers that will be    #
  # applied to all requests made by the ReportDownloader utility by default.  #
  #############################################################################
  # report_downloader_headers:
    # skip_report_header: False
    # skip_column_header: False
    # skip_report_summary: False
    # use_raw_enum_values: False
